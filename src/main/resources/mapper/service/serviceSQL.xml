<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="service">
<select id="selectOneMember" parameterType="j" resultType="j">
		select
		 member.m_name as mName,
		 member.m_phone as mPhone,
		 member.m_email as mEmail,
		 member.introduce as introduce,
		 service.s_area as sArea,
		 service.s_img as sImg,
		 service_review.r_content as rContent,
		 service_review.write_date as writeDate
		from 
		member inner join
		service on member.m_id = service.m_id left outer join
		service_review on member.m_id = service_review.m_id
		where member.m_id = 'newy12'
		<!-- 김영재 <<  -->
		</select>
		<insert id="insertService" parameterType="j">
		insert into service values(service_seq.nextval,'newy12',#{sTitle},#{sPrice},#{sContent},#{sArea},#{sImg},#{sRate},#{mainCategory},#{subCategory},#{workingDate},0,to_char(sysdate,'yyyy-mm-dd'),'y','y')
		</insert>
</mapper>
