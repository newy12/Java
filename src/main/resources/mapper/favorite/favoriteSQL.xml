<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="favorite">

	<!-- (소현) 채팅창에서 찜한 목록 불러오기 -->
	<select id="selectAllFavorite" parameterType="_int"
		resultType="s">
		select
		s_no as sNo,
		m_id as mId,
		s_title as sTitle,
		s_price as
		sPrice,
		s_content as sContent,
		s_img as sImg,
		s_rate as sRate,
		main_category as mainCategory,
		working_count as workingCount
		from
		service
		where s_no=
		(select
		s_no as sNo
		from favorite
		where m_no=#{mNo})
	</select>
</mapper>
